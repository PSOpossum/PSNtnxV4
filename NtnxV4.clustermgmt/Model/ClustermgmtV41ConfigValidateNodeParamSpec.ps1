#
# Nutanix Cluster Management APIs
# Manage Hosts, Clusters and other Infrastructure.
# Version: 4.1.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

ValidateNodeParam specs. It can be OneOf between hypervisor bundle and node uplinks.

.PARAMETER Json

JSON object

.OUTPUTS

ClustermgmtV41ConfigValidateNodeParamSpec<PSCustomObject>
#>
function ConvertFrom-JsonToClustermgmtV41ConfigValidateNodeParamSpec {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match ClustermgmtV41ConfigBundleParam defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToClustermgmtV41ConfigBundleParam $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "ClustermgmtV41ConfigBundleParam"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'ClustermgmtV41ConfigBundleParam' defined in oneOf (NtnxV4ClustermgmtV41ConfigValidateNodeParamSpec). Proceeding to the next one if any."
        }

        # try to match ClustermgmtV41ConfigUplinkNode[] defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToClustermgmtV41ConfigUplinkNode[] $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "ClustermgmtV41ConfigUplinkNode[]"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'ClustermgmtV41ConfigUplinkNode[]' defined in oneOf (NtnxV4ClustermgmtV41ConfigValidateNodeParamSpec). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([ClustermgmtV41ConfigBundleParam, ClustermgmtV41ConfigUplinkNode[]]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("ClustermgmtV41ConfigBundleParam", "ClustermgmtV41ConfigUplinkNode[]")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([ClustermgmtV41ConfigBundleParam, ClustermgmtV41ConfigUplinkNode[]]). JSON Payload: $($Json)"
        }
    }
}

