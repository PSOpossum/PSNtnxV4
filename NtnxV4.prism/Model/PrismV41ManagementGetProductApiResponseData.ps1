#
# Nutanix Prism APIs
# Manage Tasks, Category Associations and Submit Batch Operations.
# Version: 4.1.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

PrismV41ManagementGetProductApiResponseData<PSCustomObject>
#>
function ConvertFrom-NtnxV4JsonToPrismV41ManagementGetProductApiResponseData {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match PrismV41ErrorErrorResponse defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToPrismV41ErrorErrorResponse $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "PrismV41ErrorErrorResponse"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'PrismV41ErrorErrorResponse' defined in oneOf (NtnxV4PrismV41ManagementGetProductApiResponseData). Proceeding to the next one if any."
        }

        # try to match PrismV41ManagementProduct defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToPrismV41ManagementProduct $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "PrismV41ManagementProduct"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'PrismV41ManagementProduct' defined in oneOf (NtnxV4PrismV41ManagementGetProductApiResponseData). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([PrismV41ErrorErrorResponse, PrismV41ManagementProduct]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("PrismV41ErrorErrorResponse", "PrismV41ManagementProduct")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([PrismV41ErrorErrorResponse, PrismV41ManagementProduct]). JSON Payload: $($Json)"
        }
    }
}

