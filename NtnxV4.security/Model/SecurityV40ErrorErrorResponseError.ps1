#
# Nutanix Security APIs
# Manage security features, such as encryption, certificates, or platform hardening.
# Version: 4.0.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

SecurityV40ErrorErrorResponseError<PSCustomObject>
#>
function ConvertFrom-NtnxV4JsonToSecurityV40ErrorErrorResponseError {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match SecurityV40ErrorAppMessage[] defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToSecurityV40ErrorAppMessage[] $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecurityV40ErrorAppMessage[]"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecurityV40ErrorAppMessage[]' defined in oneOf (NtnxV4SecurityV40ErrorErrorResponseError). Proceeding to the next one if any."
        }

        # try to match SecurityV40ErrorSchemaValidationError defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToSecurityV40ErrorSchemaValidationError $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecurityV40ErrorSchemaValidationError"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecurityV40ErrorSchemaValidationError' defined in oneOf (NtnxV4SecurityV40ErrorErrorResponseError). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([SecurityV40ErrorAppMessage[], SecurityV40ErrorSchemaValidationError]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("SecurityV40ErrorAppMessage[]", "SecurityV40ErrorSchemaValidationError")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([SecurityV40ErrorAppMessage[], SecurityV40ErrorSchemaValidationError]). JSON Payload: $($Json)"
        }
    }
}

