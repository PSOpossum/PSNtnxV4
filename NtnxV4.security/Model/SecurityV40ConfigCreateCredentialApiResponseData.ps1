#
# Nutanix Security APIs
# Manage security features, such as encryption, certificates, or platform hardening.
# Version: 4.0.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

SecurityV40ConfigCreateCredentialApiResponseData<PSCustomObject>
#>
function ConvertFrom-NtnxV4JsonToSecurityV40ConfigCreateCredentialApiResponseData {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match PrismV41ConfigTaskReference defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToPrismV41ConfigTaskReference $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "PrismV41ConfigTaskReference"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'PrismV41ConfigTaskReference' defined in oneOf (NtnxV4SecurityV40ConfigCreateCredentialApiResponseData). Proceeding to the next one if any."
        }

        # try to match SecurityV40ErrorErrorResponse defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-NtnxV4JsonToSecurityV40ErrorErrorResponse $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecurityV40ErrorErrorResponse"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecurityV40ErrorErrorResponse' defined in oneOf (NtnxV4SecurityV40ConfigCreateCredentialApiResponseData). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([PrismV41ConfigTaskReference, SecurityV40ErrorErrorResponse]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("PrismV41ConfigTaskReference", "SecurityV40ErrorErrorResponse")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([PrismV41ConfigTaskReference, SecurityV40ErrorErrorResponse]). JSON Payload: $($Json)"
        }
    }
}

