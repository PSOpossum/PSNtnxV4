#
# Nutanix Prism APIs
# Manage Tasks, Category Associations and Submit Batch Operations.
# Version: 4.1.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Object store provider represents the object store used to store backup and trigger recovery. 

.PARAMETER Json

JSON object

.OUTPUTS

PrismV41ManagementObjectStoreLocationProviderConfig<PSCustomObject>
#>
function ConvertFrom-JsonToPrismV41ManagementObjectStoreLocationProviderConfig {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match PrismV41ManagementAWSS3Config defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToPrismV41ManagementAWSS3Config $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "PrismV41ManagementAWSS3Config"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'PrismV41ManagementAWSS3Config' defined in oneOf (PrismV41ManagementObjectStoreLocationProviderConfig). Proceeding to the next one if any."
        }

        # try to match PrismV41ManagementNutanixObjectsConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToPrismV41ManagementNutanixObjectsConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "PrismV41ManagementNutanixObjectsConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'PrismV41ManagementNutanixObjectsConfig' defined in oneOf (PrismV41ManagementObjectStoreLocationProviderConfig). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([PrismV41ManagementAWSS3Config, PrismV41ManagementNutanixObjectsConfig]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("PrismV41ManagementAWSS3Config", "PrismV41ManagementNutanixObjectsConfig")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([PrismV41ManagementAWSS3Config, PrismV41ManagementNutanixObjectsConfig]). JSON Payload: $($Json)"
        }
    }
}

