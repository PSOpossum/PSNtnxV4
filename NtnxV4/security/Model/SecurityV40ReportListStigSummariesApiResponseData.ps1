#
# Nutanix Security APIs
# Manage security features, such as encryption, certificates, or platform hardening.
# Version: 4.0.1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

SecurityV40ReportListStigSummariesApiResponseData<PSCustomObject>
#>
function ConvertFrom-JsonToSecurityV40ReportListStigSummariesApiResponseData {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match SecurityV40ErrorErrorResponse defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToSecurityV40ErrorErrorResponse $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecurityV40ErrorErrorResponse"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecurityV40ErrorErrorResponse' defined in oneOf (SecurityV40ReportListStigSummariesApiResponseData). Proceeding to the next one if any."
        }

        # try to match SecurityV40ReportStigSummary[] defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToSecurityV40ReportStigSummary[] $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecurityV40ReportStigSummary[]"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecurityV40ReportStigSummary[]' defined in oneOf (SecurityV40ReportListStigSummariesApiResponseData). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([SecurityV40ErrorErrorResponse, SecurityV40ReportStigSummary[]]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("SecurityV40ErrorErrorResponse", "SecurityV40ReportStigSummary[]")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([SecurityV40ErrorErrorResponse, SecurityV40ReportStigSummary[]]). JSON Payload: $($Json)"
        }
    }
}

